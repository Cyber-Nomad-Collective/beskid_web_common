/**
 * Placeholder remark plugin for future **markdown-first** hub widgets
 * (fenced `trudoc-hub` blocks → HTML shells + progressive enhancement).
 *
 * Register via `trudoc({ remarkPlugins: [trudocHubFence()] })` when implemented.
 */
export default function trudocHubFence(): () => void {
	return () => {
		/* no-op */
	};
}
