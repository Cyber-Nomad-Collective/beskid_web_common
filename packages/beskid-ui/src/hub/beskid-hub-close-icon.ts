/** Close control SVG for the hub header (shared across Astro / React / Blazor). */

export const HUB_CLOSE_ICON_SVG = `<svg viewBox="0 0 24 24" width="20" height="20" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"></path></svg>`;
