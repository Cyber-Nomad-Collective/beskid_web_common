# @beskid/docs-ui

Astro/React components and styles for Beskid **platform-spec** and **book** reader chrome. Requires `@cyber-nomad-collective/trudoc` and an Astro/Starlight consumer (`site/website`).

Typechecking is intended to run from the docs site (`astro check` / site `tsc`), not in isolation.
